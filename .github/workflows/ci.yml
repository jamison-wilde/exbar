name: CI

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]

# Prevent duplicate runs: a tag push on main would otherwise trigger
# both the branch and tag workflows. This cancels the branch run.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  lint:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets -- -D warnings

  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --all-targets

  doc-check:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo doc --no-deps --workspace
        env:
          RUSTDOCFLAGS: "-D warnings"

  build-msi:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [lint, test, doc-check]
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo build --release -p exbar-cli
      - run: dotnet tool install --global wix
      - run: wix eula accept wix7
      - run: wix extension add --global WixToolset.Util.wixext WixToolset.UI.wixext
      - name: Build MSI
        run: bash ./scripts/build-msi.sh
      - name: Upload MSI artifact
        uses: actions/upload-artifact@v4
        with:
          name: exbar-msi
          path: target/wix/exbar-*.msi
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: target/wix/exbar-*.msi
          generate_release_notes: true
